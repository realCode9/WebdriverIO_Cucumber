import { toHaveHTML } from './toHaveHTML.js';
export function toHaveHTMLContaining(el, html, options = {}) {
    return toHaveHTML.call(this, el, html, {
        ...options,
        containing: true
    });
}
