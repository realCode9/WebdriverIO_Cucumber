import { toHaveComputedLabel } from './toHaveComputedLabel.js';
export function toHaveComputedLabelContaining(el, label, options = {}) {
    return toHaveComputedLabel.call(this, el, label, {
        ...options,
        containing: true
    });
}
