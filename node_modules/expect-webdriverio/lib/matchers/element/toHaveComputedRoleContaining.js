import { toHaveComputedRole } from './toHaveComputedRole.js';
export function toHaveComputedRoleContaining(el, role, options = {}) {
    return toHaveComputedRole.call(this, el, role, {
        ...options,
        containing: true
    });
}
